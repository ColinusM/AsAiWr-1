#!/bin/bash
"""
Quick launcher for Voice Interrupt GUI
Usage: ./launch_interrupt_gui
"""

echo "🗣️ Launching Voice Interrupt GUI..."
echo "⏹️  This will add a STOP button for voice conversations"
echo ""

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Launch the GUI
python3 "$SCRIPT_DIR/voice_interrupt_gui.py" &

echo "✅ GUI launched in background (top-right corner)"
echo "💡 Now use Claude Code's /voice-mode:converse command"
echo "💡 Press SPACE in GUI or click STOP to interrupt audio"
echo ""
echo "To kill GUI later: killall python3"